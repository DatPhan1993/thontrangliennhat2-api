{
  "name": "thontrangliennhat-api",
  "version": "1.0.0",
  "description": "API server for Thôn Trang Liên Nhật website",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "node server-express.js",
    "dev": "nodemon server-express.js",
    "build": "node copy-db.js && node copy-images.js",
    "vercel-build": "node copy-db.js && node copy-images.js",
    "test": "echo \"No tests specified\" && exit 0",
    "lint": "echo \"No linting specified\" && exit 0"
  },
  "keywords": [
    "api",
    "nodejs",
    "express",
    "serverless"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.2",
    "slugify": "^1.6.6",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
} 